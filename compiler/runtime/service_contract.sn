استورد compiler.runtime.service_identity;

// فئة ناتج مجردة لا تفرض تمثيل ذاكرة أو ABI أو حجما هدفيا.
عام نوع فئة_ناتج_خدمة_تشغيل =
    | بلا_قيمة
    | عدد_صحيح
    | مرجع_ذاكرة
;

عام نوع عقد_خدمة_تشغيل = {
    أقل_عدد_حجج: غير_موقع,
    أكبر_عدد_حجج: غير_موقع,
    فئة_الناتج: فئة_ناتج_خدمة_تشغيل,
    قد_تفشل: منطقي,
    قد_تخصص: منطقي
};

عرف أنشئ_عقد_خدمة_تشغيل(
    الأقل: غير_موقع,
    الأكبر: غير_موقع,
    الناتج: فئة_ناتج_خدمة_تشغيل,
    قد_تفشل: منطقي,
    قد_تخصص: منطقي
): عقد_خدمة_تشغيل {
    دع العقد: عقد_خدمة_تشغيل = {
        أقل_عدد_حجج: الأقل,
        أكبر_عدد_حجج: الأكبر,
        فئة_الناتج: الناتج,
        قد_تفشل: قد_تفشل,
        قد_تخصص: قد_تخصص
    };
    أرجع العقد;
}

// العقد هنا دلالية ومحايدة للمنصة؛ لا تحتوي أسماء مضيف أو أرقام نداءات
// نظام أو رموز مكتبات أو تفاصيل واجهة ثنائية.
عام عرف عقد_خدمة_تشغيل_للهوية(الخدمة: هوية_خدمة_تشغيل): عقد_خدمة_تشغيل {
    طابق الخدمة مع
    | هوية_خدمة_تشغيل.تخصيص -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 2, فئة_ناتج_خدمة_تشغيل.مرجع_ذاكرة(), صواب, صواب);
    }
    | هوية_خدمة_تشغيل.إعادة_تخصيص -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(3, 4, فئة_ناتج_خدمة_تشغيل.مرجع_ذاكرة(), صواب, صواب);
    }
    | هوية_خدمة_تشغيل.تحرير -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.بلا_قيمة(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.فتح_ملف -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.قراءة_ملف -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.كتابة_ملف -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.إنشاء_ملف -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.إغلاق_ملف -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.فتح_دليل -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.قراءة_مدخل_دليل -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.إغلاق_دليل -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.قراءة_الدخل_القياسي -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.كتابة_الخرج_القياسي -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.كتابة_الخطأ_القياسي -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.عدد_الوسائط -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(0, 0, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), خطأ, خطأ);
    }
    | هوية_خدمة_تشغيل.الوصول_إلى_وسيط -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.مرجع_ذاكرة(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.خروج_العملية -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.بلا_قيمة(), خطأ, خطأ);
    }
    | هوية_خدمة_تشغيل.كتابة_تشخيص -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ);
    }
    | هوية_خدمة_تشغيل.فشل_وقت_تشغيل -> {
        أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.بلا_قيمة(), خطأ, خطأ);
    }
    | هوية_خدمة_تشغيل.تصنيف_خطأ_نظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), خطأ, خطأ); }
    | هوية_خدمة_تشغيل.إنشاء_دليل -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.إزالة_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.إزالة_دليل -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.إعادة_تسمية -> { أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.نوع_مسار -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.حجم_مسار -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.وقت_تعديل_مسار -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.فتح_مقبض_ملف_للقراءة -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, صواب); }
    | هوية_خدمة_تشغيل.إنشاء_مقبض_ملف_للكتابة -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, صواب); }
    | هوية_خدمة_تشغيل.قراءة_مقطع_مقبض_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(4, 4, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.كتابة_مقطع_مقبض_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(4, 4, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.إغلاق_مقبض_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.تحريك_مقبض_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.موضع_مقبض_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.مزامنة_مقبض_ملف -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.وقت_رتيب_نانو -> { أرجع أنشئ_عقد_خدمة_تشغيل(0, 0, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.وقت_نظام_نانو -> { أرجع أنشئ_عقد_خدمة_تشغيل(0, 0, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.نوم_نانو -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.طول_متغير_بيئة -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.قراءة_متغير_بيئة -> { أرجع أنشئ_عقد_خدمة_تشغيل(4, 4, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.دليل_العمل -> { أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.الدليل_المؤقت -> { أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.تشغيل_عملية -> { أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, صواب); }
    | هوية_خدمة_تشغيل.انتظار_عملية -> { أرجع أنشئ_عقد_خدمة_تشغيل(1, 1, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.حجم_صفحة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(0, 0, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), خطأ, خطأ); }
    | هوية_خدمة_تشغيل.خريطة_ذاكرة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, صواب); }
    | هوية_خدمة_تشغيل.إلغاء_خريطة_ذاكرة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.حماية_ذاكرة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.قفل_ذاكرة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.إلغاء_قفل_ذاكرة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(2, 2, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    | هوية_خدمة_تشغيل.نصيحة_ذاكرة_النظام -> { أرجع أنشئ_عقد_خدمة_تشغيل(3, 3, فئة_ناتج_خدمة_تشغيل.عدد_صحيح(), صواب, خطأ); }
    ;
}
