استورد compiler.frontend.names.identifiers;
استورد compiler.frontend.names.module_identity;
استورد compiler.frontend.names.symbols;
استورد compiler.frontend.names.scopes;
استورد compiler.frontend.source.source;
استورد compiler.frontend.source.position;
استورد compiler.frontend.source.scope;
استورد compiler.frontend.identifier_identity;
استورد compiler.runtime.primitive_identity;

// ربط اسم تابع داخل فضاء أسماء رمز مالك. يمكن لعدة أسماء أن تشير إلى نفس الرمز التابع.
عام نوع ربط_اسم_تابع = {
    المالك: معرف_رمز,
    الاسم: معرف_اسم,
    الرمز: معرف_رمز
};

// ربط التهجئة المصدرية للبدائي لا يملك هوية تشغيلية؛ الرمز وحده هو الهوية الدلالية.
عام نوع ربط_اسم_بدائي_مترجم = {
    الاسم: معرف_اسم,
    الرمز: معرف_رمز
};

// حقيقة دلالية واحدة لكل رمز بدائي. Runtime هو مالك نوع الهوية نفسه.
عام نوع ربط_رمز_بدائي_مترجم = {
    الرمز: معرف_رمز,
    الهوية: هوية_بدائي_مترجم
};

عام نوع مدخل_فهرس_اسم = {
    الهاش: غير_موقع,
    الاسم: معرف_اسم
};

// Name System هو صاحب الحقيقة الوحيد لهويات الأسماء المقننة. الرموز لا تنسخ مصدرها.
// Compact persistent Name facts for semantic-query consumers. The full Name context
// remains phase-local; tooling requests retain only canonical Symbol facts.
عام نوع سياق_استعلامات_أسماء = {
    النطاقات: متجه نطاق_أسماء,
    الأسماء: متجه اسم_مقنن,
    الرموز: متجه رمز_دلالي,
    الروابط_التابعة: متجه ربط_اسم_تابع
};

عام نوع سياق_أسماء = {
    النطاق_التالي: غير_موقع,
    الاسم_التالي: غير_موقع,
    الرمز_التالي: غير_موقع,
    النطاقات: متجه نطاق_أسماء,
    الأسماء: متجه اسم_مقنن,
    فهرس_الأسماء: متجه اختيار مدخل_فهرس_اسم,
    الرموز: متجه رمز_دلالي,
    الروابط_التابعة: متجه ربط_اسم_تابع,
    أسماء_البدائيات: متجه ربط_اسم_بدائي_مترجم,
    هويات_البدائيات: متجه ربط_رمز_بدائي_مترجم
};

عام عرف سياق_أسماء_جديد(): سياق_أسماء {
    دع النطاقات: متجه نطاق_أسماء = [];
    دع الأسماء: متجه اسم_مقنن = [];
    دع فهرس_الأسماء: متجه اختيار مدخل_فهرس_اسم = [];
    دع الرموز: متجه رمز_دلالي = [];
    دع الروابط_التابعة: متجه ربط_اسم_تابع = [];
    دع أسماء_البدائيات: متجه ربط_اسم_بدائي_مترجم = [];
    دع هويات_البدائيات: متجه ربط_رمز_بدائي_مترجم = [];
    دع سياق_الأسماء: سياق_أسماء = {
        النطاق_التالي: 0,
        الاسم_التالي: 0,
        الرمز_التالي: 0,
        النطاقات: النطاقات,
        الأسماء: الأسماء,
        فهرس_الأسماء: فهرس_الأسماء,
        الرموز: الرموز,
        الروابط_التابعة: الروابط_التابعة,
        أسماء_البدائيات: أسماء_البدائيات,
        هويات_البدائيات: هويات_البدائيات
    };
    أرجع سياق_الأسماء;
}

عرف أضف_نطاقا(
    سياق_الأسماء: &متغير سياق_أسماء,
    الوحدة: معرف_وحدة,
    الأب: اختيار معرف_نطاق
): معرف_نطاق {
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    دع المعرف: معرف_نطاق = معرف_نطاق_من(الحالة.النطاق_التالي);
    دع الرموز: متجه معرف_رمز = [];
    دع الروابط: متجه ربط_اسم_نطاق = [];
    دع بلا_نطاق_مصدر: اختيار نطاق = اختيار.لا_شيء();
    دع النطاق: نطاق_أسماء = {
        المعرف: المعرف,
        الأب: الأب,
        الوحدة: الوحدة,
        نطاق_المصدر: بلا_نطاق_مصدر,
        الرموز: الرموز,
        الروابط: الروابط
    };
    ألحق(الحالة.النطاقات, النطاق);
    الحالة.النطاق_التالي := الحالة.النطاق_التالي + 1;
    *سياق_الأسماء := الحالة;
    أرجع المعرف;
}

عام عرف نطاق_جذري(
    سياق_الأسماء: &متغير سياق_أسماء,
    الوحدة: معرف_وحدة
): معرف_نطاق {
    أرجع أضف_نطاقا(سياق_الأسماء, الوحدة, اختيار.لا_شيء());
}

عام عرف نطاق_ابن(
    سياق_الأسماء: &متغير سياق_أسماء,
    الأب: معرف_نطاق
): معرف_نطاق {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    دع أصل_مرجع: &نطاق_أسماء = &الحالة.النطاقات[الأب.القيمة];
    دع الأصل: نطاق_أسماء = *أصل_مرجع;
    أرجع أضف_نطاقا(سياق_الأسماء, الأصل.الوحدة, اختيار.بعض(الأب));
}

// Source containment is a canonical Name fact used by tooling to locate the
// active lexical scope without reconstructing scopes from syntax.
عام عرف ضع_نطاق_مصدر_لنطاق(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق: معرف_نطاق,
    نطاق_المصدر: نطاق
): فراغ {
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) { أرجع; };
    الحالة.النطاقات[النطاق.القيمة].نطاق_المصدر := اختيار.بعض(نطاق_المصدر);
    *سياق_الأسماء := الحالة;
}

عام عرف نطاق_ابن_بمصدر(
    سياق_الأسماء: &متغير سياق_أسماء,
    الأب: معرف_نطاق,
    نطاق_المصدر: نطاق
): معرف_نطاق {
    دع المعرف: معرف_نطاق = نطاق_ابن(سياق_الأسماء, الأب);
    ضع_نطاق_مصدر_لنطاق(سياق_الأسماء, المعرف, نطاق_المصدر);
    أرجع المعرف;
}

عرف ابن_فهرس_أسماء_فارغا(السعة: غير_موقع): متجه اختيار مدخل_فهرس_اسم {
    دع الناتج: متغير متجه اختيار مدخل_فهرس_اسم = [];
    دع ف: متغير غير_موقع = 0;
    بينما (ف < السعة) {
        ألحق(الناتج, اختيار.لا_شيء());
        ف := ف + 1;
    }
    أرجع الناتج;
}

عرف أدرج_مدخل_فهرس_اسم(
    الفهرس: &متغير متجه اختيار مدخل_فهرس_اسم,
    المدخل: مدخل_فهرس_اسم
): فراغ {
    دع الحالة: متغير متجه اختيار مدخل_فهرس_اسم = *الفهرس;
    دع السعة: غير_موقع = الطول(الحالة) مثل غير_موقع;
    لو (السعة == 0) { أرجع; };
    دع الموضع: متغير غير_موقع = المدخل.الهاش % السعة;
    بينما (صواب) {
        طابق الحالة[الموضع] مع
        | اختيار.لا_شيء -> {
            الحالة[الموضع] := اختيار.بعض(المدخل);
            *الفهرس := الحالة;
            أرجع;
        }
        | اختيار.بعض(_) -> {
            الموضع := (الموضع + 1) % السعة;
        };
    }
}

عرف تأكد_سعة_فهرس_الأسماء(سياق_الأسماء: &متغير سياق_أسماء): فراغ {
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    دع العدد: غير_موقع = الطول(الحالة.الأسماء) مثل غير_موقع;
    دع القديمة: غير_موقع = الطول(الحالة.فهرس_الأسماء) مثل غير_موقع;
    دع تحتاج: متغير منطقي = القديمة == 0;
    لو (!تحتاج) {
        تحتاج := (العدد * 3) >= (القديمة * 2);
    };
    لو (!تحتاج) { أرجع; };
    دع الجديدة: متغير غير_موقع = 16;
    لو (القديمة > 0) { الجديدة := القديمة * 2; };
    دع الفهرس_الجديد: متغير متجه اختيار مدخل_فهرس_اسم = ابن_فهرس_أسماء_فارغا(الجديدة);
    دع ف: متغير غير_موقع = 0;
    بينما (ف < القديمة) {
        طابق الحالة.فهرس_الأسماء[ف] مع
        | اختيار.بعض(المدخل) -> {
            أدرج_مدخل_فهرس_اسم(&متغير الفهرس_الجديد, المدخل);
        }
        | اختيار.لا_شيء -> { };
        ف := ف + 1;
    }
    الحالة.فهرس_الأسماء := الفهرس_الجديد;
    *سياق_الأسماء := الحالة;
}

عرف ابحث_عن_هوية(
    سياق_الأسماء: &سياق_أسماء,
    الهوية: &متجه غ8
): اختيار معرف_اسم {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    دع السعة: غير_موقع = الطول(الحالة.فهرس_الأسماء) مثل غير_موقع;
    لو (السعة == 0) { أرجع اختيار.لا_شيء(); };
    دع الهاش: غير_موقع = هاش_هوية_معرف_بايتات(الهوية);
    دع الموضع: متغير غير_موقع = الهاش % السعة;
    دع المفحوص: متغير غير_موقع = 0;
    بينما (المفحوص < السعة) {
        طابق الحالة.فهرس_الأسماء[الموضع] مع
        | اختيار.لا_شيء -> { أرجع اختيار.لا_شيء(); }
        | اختيار.بعض(المدخل) -> {
            لو (المدخل.الهاش == الهاش
                && المدخل.الاسم.القيمة < (الطول(الحالة.الأسماء) مثل غير_موقع)
                && هويتان_معرفيتان_متساويتان(&الحالة.الأسماء[المدخل.الاسم.القيمة].البايتات, الهوية)) {
                أرجع اختيار.بعض(المدخل.الاسم);
            };
        };
        الموضع := (الموضع + 1) % السعة;
        المفحوص := المفحوص + 1;
    }
    أرجع اختيار.لا_شيء();
}

عرف ابحث_عن_هوية_في_شريحة(
    سياق_الأسماء: &سياق_أسماء,
    المصدر: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): اختيار معرف_اسم {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    دع السعة: غير_موقع = الطول(الحالة.فهرس_الأسماء) مثل غير_موقع;
    لو (السعة == 0) { أرجع اختيار.لا_شيء(); };
    دع الهاش: غير_موقع = هاش_هوية_معرف_في_شريحة(المصدر, البداية, النهاية);
    دع الموضع: متغير غير_موقع = الهاش % السعة;
    دع المفحوص: متغير غير_موقع = 0;
    بينما (المفحوص < السعة) {
        طابق الحالة.فهرس_الأسماء[الموضع] مع
        | اختيار.لا_شيء -> { أرجع اختيار.لا_شيء(); }
        | اختيار.بعض(المدخل) -> {
            لو (المدخل.الهاش == الهاش
                && المدخل.الاسم.القيمة < (الطول(الحالة.الأسماء) مثل غير_موقع)
                && تطابق_هوية_بايتات_وشريحة(
                    &الحالة.الأسماء[المدخل.الاسم.القيمة].البايتات,
                    المصدر, البداية, النهاية
                )) {
                أرجع اختيار.بعض(المدخل.الاسم);
            };
        };
        الموضع := (الموضع + 1) % السعة;
        المفحوص := المفحوص + 1;
    }
    أرجع اختيار.لا_شيء();
}

// Generic read-only text -> canonical Name identity lookup. Primitive classification
// remains identity-based after registration; this query does not know primitive spellings.
عام عرف هوية_اسم_لنص_إن_وجد(
    سياق_الأسماء: &سياق_أسماء,
    النص: &نص
): اختيار معرف_اسم {
    دع الأصل: نص = *النص;
    دع النهاية: غير_موقع = الطول(الأصل) مثل غير_موقع;
    أرجع ابحث_عن_هوية_في_شريحة(سياق_الأسماء, &الأصل, 0, النهاية);
}

عرف سجل_اسما(سياق_الأسماء: &متغير سياق_أسماء, البايتات: متجه غ8): معرف_اسم {
    دع الهاش: غير_موقع = هاش_هوية_معرف_بايتات(&البايتات);
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    دع المعرف: معرف_اسم = معرف_اسم_من(الحالة.الاسم_التالي);
    دع الاسم: اسم_مقنن = { المعرف: المعرف, البايتات: البايتات };
    ألحق(الحالة.الأسماء, الاسم);
    الحالة.الاسم_التالي := الحالة.الاسم_التالي + 1;
    *سياق_الأسماء := الحالة;
    تأكد_سعة_فهرس_الأسماء(سياق_الأسماء);
    دع محدثة: متغير سياق_أسماء = *سياق_الأسماء;
    دع المدخل: مدخل_فهرس_اسم = { الهاش: الهاش, الاسم: المعرف };
    أدرج_مدخل_فهرس_اسم(&متغير محدثة.فهرس_الأسماء, المدخل);
    *سياق_الأسماء := محدثة;
    أرجع المعرف;
}

عرف قنن_اسما_نصيا(سياق_الأسماء: &متغير سياق_أسماء, النص: &نص): معرف_اسم {
    دع الأصل: نص = *النص;
    دع النهاية: غير_موقع = الطول(الأصل) مثل غير_موقع;
    دع الموجود: اختيار معرف_اسم = ابحث_عن_هوية_في_شريحة(سياق_الأسماء, &الأصل, 0, النهاية);
    طابق الموجود مع
    | اختيار.بعض(المعرف) -> { أرجع المعرف; }
    | اختيار.لا_شيء -> { };
    دع البايتات: متجه غ8 = انسخ_هوية_معرف_من_نص(&الأصل);
    أرجع سجل_اسما(سياق_الأسماء, البايتات);
}

عام عرف قنن_اسما_مصدريا(
    سياق_الأسماء: &متغير سياق_أسماء,
    المصدر: &مصدر,
    النطاق: نطاق
): معرف_اسم {
    دع الأصل: مصدر = *المصدر;
    دع الموجود: اختيار معرف_اسم = ابحث_عن_هوية_في_شريحة(
        سياق_الأسماء,
        &الأصل.المحتوى,
        النطاق.البداية.البايت,
        النطاق.النهاية.البايت
    );
    طابق الموجود مع
    | اختيار.بعض(المعرف) -> { أرجع المعرف; }
    | اختيار.لا_شيء -> { };
    دع البايتات: متجه غ8 = انسخ_هوية_معرف_من_شريحة(
        &الأصل.المحتوى,
        النطاق.البداية.البايت,
        النطاق.النهاية.البايت
    );
    أرجع سجل_اسما(سياق_الأسماء, البايتات);
}

عام عرف ابحث_محليا_بهوية(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: معرف_اسم
): اختيار معرف_رمز {
    أرجع ابحث_محليا_بهوية_في_حيز(
        سياق_الأسماء, النطاق, الاسم, حيز_اسم.الكل()
    );
}

عام عرف ابحث_محليا(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: &نص
): اختيار معرف_رمز {
    دع الأصل: نص = *الاسم;
    دع النهاية: غير_موقع = الطول(الأصل) مثل غير_موقع;
    دع هوية: اختيار معرف_اسم = ابحث_عن_هوية_في_شريحة(سياق_الأسماء, &الأصل, 0, النهاية);
    طابق هوية مع
    | اختيار.لا_شيء -> {
        أرجع اختيار.لا_شيء();
    }
    | اختيار.بعض(المعرف) -> {
        أرجع ابحث_محليا_بهوية(سياق_الأسماء, النطاق, المعرف);
    };
}

عرف ابحث_من(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: &نص
): اختيار معرف_رمز {
    دع المحلي: اختيار معرف_رمز = ابحث_محليا(سياق_الأسماء, النطاق, الاسم);
    طابق المحلي مع
    | اختيار.بعض(المعرف) -> {
        أرجع اختيار.بعض(المعرف);
    }
    | اختيار.لا_شيء -> {
    };
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    دع مرجع_سجل_النطاق: &نطاق_أسماء = &الحالة.النطاقات[النطاق.القيمة];
    دع سجل_النطاق: نطاق_أسماء = *مرجع_سجل_النطاق;
    طابق سجل_النطاق.الأب مع
    | اختيار.لا_شيء -> {
        أرجع اختيار.لا_شيء();
    }
    | اختيار.بعض(الأب) -> {
        أرجع ابحث_من(سياق_الأسماء, الأب, الاسم);
    };
}

عام عرف ابحث(
    سياق_الأسماء: &سياق_أسماء,
    البداية: معرف_نطاق,
    الاسم: &نص
): اختيار معرف_رمز {
    أرجع ابحث_من(سياق_الأسماء, البداية, الاسم);
}

// Identity-preserving lexical lookup for already-canonical source occurrences.
عام عرف ابحث_من_بهوية(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: معرف_اسم
): اختيار معرف_رمز {
    أرجع ابحث_من_بهوية_في_حيز(
        سياق_الأسماء, النطاق, الاسم, حيز_اسم.الكل()
    );
}

// Context-filtered lexical lookup. A same-spelling binding in the other contextual
// namespace does not shadow the requested declaration class.
عام عرف ابحث_محليا_بهوية_في_حيز(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: معرف_اسم,
    الحيز: حيز_اسم
): اختيار معرف_رمز {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    دع مرجع_النطاق: &نطاق_أسماء = &الحالة.النطاقات[النطاق.القيمة];
    دع موضع_النطاق: نطاق_أسماء = *مرجع_النطاق;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(موضع_النطاق.الروابط) مثل غير_موقع)) {
        دع الربط: ربط_اسم_نطاق = موضع_النطاق.الروابط[ف];
        لو (معرفا_اسم_متساويان(الربط.الاسم, الاسم)) {
            طابق نوع_رمز_عند(سياق_الأسماء, الربط.الرمز) مع
            | اختيار.بعض(النوع) -> {
                لو (هل_نوع_تصريح_في_حيز(النوع, الحيز)) {
                    أرجع اختيار.بعض(الربط.الرمز);
                };
            }
            | اختيار.لا_شيء -> { };
        };
        ف := ف + 1;
    }
    أرجع اختيار.لا_شيء();
}

عام عرف ابحث_محليا_ببايتات_في_حيز(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الهوية: &متجه غ8,
    الحيز: حيز_اسم
): اختيار معرف_رمز {
    // The global Name interner already owns the byte -> NameID hash index.
    // Query it read-only, then resolve the canonical identity in the lexical scope;
    // do not re-compare the same spelling against every binding in the scope.
    طابق ابحث_عن_هوية(سياق_الأسماء, الهوية) مع
    | اختيار.لا_شيء -> { أرجع اختيار.لا_شيء(); }
    | اختيار.بعض(الاسم) -> {
        أرجع ابحث_محليا_بهوية_في_حيز(سياق_الأسماء, النطاق, الاسم, الحيز);
    };
}

عام عرف ابحث_من_ببايتات_في_حيز(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الهوية: &متجه غ8,
    الحيز: حيز_اسم
): اختيار معرف_رمز {
    طابق ابحث_محليا_ببايتات_في_حيز(سياق_الأسماء, النطاق, الهوية, الحيز) مع
    | اختيار.بعض(الرمز) -> {
        أرجع اختيار.بعض(الرمز);
    }
    | اختيار.لا_شيء -> { };
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    طابق الحالة.النطاقات[النطاق.القيمة].الأب مع
    | اختيار.لا_شيء -> {
        أرجع اختيار.لا_شيء();
    }
    | اختيار.بعض(الأب) -> {
        أرجع ابحث_من_ببايتات_في_حيز(سياق_الأسماء, الأب, الهوية, الحيز);
    };
}

عام عرف ابحث_بدائيا_ببايتات(
    سياق_الأسماء: &سياق_أسماء,
    الهوية: &متجه غ8
): اختيار معرف_رمز {
    طابق ابحث_عن_هوية(سياق_الأسماء, الهوية) مع
    | اختيار.لا_شيء -> { أرجع اختيار.لا_شيء(); }
    | اختيار.بعض(الاسم) -> {
        أرجع ابحث_رمزا_بدائيا_بهوية(سياق_الأسماء, الاسم);
    };
}

عام عرف ابحث_من_بهوية_في_حيز(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: معرف_اسم,
    الحيز: حيز_اسم
): اختيار معرف_رمز {
    طابق ابحث_محليا_بهوية_في_حيز(سياق_الأسماء, النطاق, الاسم, الحيز) مع
    | اختيار.بعض(المعرف) -> {
        أرجع اختيار.بعض(المعرف);
    }
    | اختيار.لا_شيء -> { };
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    طابق الحالة.النطاقات[النطاق.القيمة].الأب مع
    | اختيار.لا_شيء -> {
        أرجع اختيار.لا_شيء();
    }
    | اختيار.بعض(الأب) -> {
        أرجع ابحث_من_بهوية_في_حيز(سياق_الأسماء, الأب, الاسم, الحيز);
    };
}

عرف خصص_رمزا(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: معرف_اسم,
    نطاق_الاسم: نطاق,
    نطاق_التصريح: نطاق,
    المالك: اختيار معرف_رمز,
    النوع: نوع_تصريح,
    علني: منطقي,
    داخل_النطاق: منطقي
): معرف_رمز {
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    دع المعرف: معرف_رمز = معرف_رمز_من(الحالة.الرمز_التالي);
    دع الرمز: رمز_دلالي = {
        المعرف: المعرف,
        الاسم: الاسم,
        نطاق_الاسم: نطاق_الاسم,
        نطاق_التصريح: نطاق_التصريح,
        النطاق_المالك: النطاق,
        المالك: المالك,
        النوع: النوع,
        علني: علني
    };
    ألحق(الحالة.الرموز, الرمز);
    لو (داخل_النطاق) {
        دع الربط: ربط_اسم_نطاق = { الاسم: الاسم, الرمز: المعرف };
        ألحق(الحالة.النطاقات[النطاق.القيمة].الرموز, المعرف);
        ألحق(الحالة.النطاقات[النطاق.القيمة].الروابط, الربط);
    };
    الحالة.الرمز_التالي := الحالة.الرمز_التالي + 1;
    *سياق_الأسماء := الحالة;
    أرجع المعرف;
}

عام عرف أضف_رمزا(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق: معرف_نطاق,
    الاسم: نص,
    نطاق_التصريح: نطاق,
    النوع: نوع_تصريح,
    علني: منطقي
): نتيجة_إضافة {
    دع الهوية: معرف_اسم = قنن_اسما_نصيا(سياق_الأسماء, &الاسم);
    دع الموجود: اختيار معرف_رمز = ابحث_محليا_بهوية(سياق_الأسماء, النطاق, الهوية);
    طابق الموجود مع
    | اختيار.بعض(المعرف) -> {
        أرجع نتيجة_إضافة.مكرر(المعرف, نطاق_التصريح);
    }
    | اختيار.لا_شيء -> {
    };
    دع المعرف: معرف_رمز = خصص_رمزا(
        سياق_الأسماء, النطاق, الهوية, نطاق_التصريح, نطاق_التصريح,
        اختيار.لا_شيء(), النوع, علني, صواب
    );
    أرجع نتيجة_إضافة.أضيف(المعرف);
}

عام عرف أضف_رمزا_مصدريا(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق: معرف_نطاق,
    المصدر: &مصدر,
    نطاق_الاسم: نطاق,
    نطاق_التصريح: نطاق,
    النوع: نوع_تصريح,
    علني: منطقي
): نتيجة_إضافة {
    دع الهوية: معرف_اسم = قنن_اسما_مصدريا(سياق_الأسماء, المصدر, نطاق_الاسم);
    دع الموجود: اختيار معرف_رمز = ابحث_محليا_بهوية(سياق_الأسماء, النطاق, الهوية);
    طابق الموجود مع
    | اختيار.بعض(المعرف) -> {
        أرجع نتيجة_إضافة.مكرر(المعرف, نطاق_التصريح);
    }
    | اختيار.لا_شيء -> {
    };
    دع المعرف: معرف_رمز = خصص_رمزا(
        سياق_الأسماء, النطاق, الهوية, نطاق_الاسم, نطاق_التصريح,
        اختيار.لا_شيء(), النوع, علني, صواب
    );
    أرجع نتيجة_إضافة.أضيف(المعرف);
}

// يسجل تهجئة إضافية للرمز نفسه داخل سجل أسماء النطاق المقنن.
// لا ينشئ رمزاً جديداً ولا يكرر هوية التصريح.
عام عرف أضف_اسما_بديلا_مصدريا(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق: معرف_نطاق,
    المصدر: &مصدر,
    نطاق_الاسم: نطاق,
    الرمز: معرف_رمز
): نتيجة_إضافة {
    دع الهوية: معرف_اسم = قنن_اسما_مصدريا(سياق_الأسماء, المصدر, نطاق_الاسم);
    دع الموجود: اختيار معرف_رمز = ابحث_محليا_بهوية(سياق_الأسماء, النطاق, الهوية);
    طابق الموجود مع
    | اختيار.بعض(المعرف) -> {
        أرجع نتيجة_إضافة.مكرر(المعرف, نطاق_الاسم);
    }
    | اختيار.لا_شيء -> {
    };
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    لو (الرمز.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع نتيجة_إضافة.مكرر(الرمز, نطاق_الاسم);
    };
    دع الربط: ربط_اسم_نطاق = { الاسم: الهوية, الرمز: الرمز };
    ألحق(الحالة.النطاقات[النطاق.القيمة].الروابط, الربط);
    *سياق_الأسماء := الحالة;
    أرجع نتيجة_إضافة.أضيف(الرمز);
}

عام عرف أضف_رمزا_تابعا_مصدريا(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق: معرف_نطاق,
    المالك: معرف_رمز,
    المصدر: &مصدر,
    نطاق_الاسم: نطاق,
    نطاق_التصريح: نطاق,
    النوع: نوع_تصريح,
    علني: منطقي
): نتيجة_إضافة {
    دع الهوية: معرف_اسم = قنن_اسما_مصدريا(سياق_الأسماء, المصدر, نطاق_الاسم);
    طابق ابحث_رمزا_تابعا_بهوية(سياق_الأسماء, المالك, الهوية) مع
    | اختيار.بعض(الموجود) -> {
        أرجع نتيجة_إضافة.مكرر(الموجود, نطاق_التصريح);
    }
    | اختيار.لا_شيء -> { };
    دع المعرف: معرف_رمز = خصص_رمزا(
        سياق_الأسماء, النطاق, الهوية, نطاق_الاسم, نطاق_التصريح,
        اختيار.بعض(المالك), النوع, علني, خطأ
    );
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    دع الربط: ربط_اسم_تابع = { المالك: المالك, الاسم: الهوية, الرمز: المعرف };
    ألحق(الحالة.الروابط_التابعة, الربط);
    *سياق_الأسماء := الحالة;
    أرجع نتيجة_إضافة.أضيف(المعرف);
}

// يضيف تهجئة أخرى إلى فضاء أسماء المالك نفسه مع الحفاظ على هوية الرمز التابع.
عام عرف أضف_اسما_بديلا_تابعا_مصدريا(
    سياق_الأسماء: &متغير سياق_أسماء,
    المالك: معرف_رمز,
    المصدر: &مصدر,
    نطاق_الاسم: نطاق,
    الرمز: معرف_رمز
): نتيجة_إضافة {
    دع الهوية: معرف_اسم = قنن_اسما_مصدريا(سياق_الأسماء, المصدر, نطاق_الاسم);
    طابق ابحث_رمزا_تابعا_بهوية(سياق_الأسماء, المالك, الهوية) مع
    | اختيار.بعض(الموجود) -> {
        أرجع نتيجة_إضافة.مكرر(الموجود, نطاق_الاسم);
    }
    | اختيار.لا_شيء -> { };
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    لو (الرمز.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع نتيجة_إضافة.مكرر(الرمز, نطاق_الاسم);
    };
    دع رمز_الابن: رمز_دلالي = الحالة.الرموز[الرمز.القيمة];
    طابق رمز_الابن.المالك مع
    | اختيار.بعض(مالك_الرمز) -> {
        لو (!معرفا_رمز_متساويان(مالك_الرمز, المالك)) {
            أرجع نتيجة_إضافة.مكرر(الرمز, نطاق_الاسم);
        };
    }
    | اختيار.لا_شيء -> {
        أرجع نتيجة_إضافة.مكرر(الرمز, نطاق_الاسم);
    };
    دع الربط: ربط_اسم_تابع = { المالك: المالك, الاسم: الهوية, الرمز: الرمز };
    ألحق(الحالة.الروابط_التابعة, الربط);
    *سياق_الأسماء := الحالة;
    أرجع نتيجة_إضافة.أضيف(الرمز);
}

// Read-only canonical child-symbol lookup. Parent Symbol identity owns the child namespace.
عام عرف ابحث_رمزا_تابعا_بهوية(
    سياق_الأسماء: &سياق_أسماء,
    المالك: معرف_رمز,
    الاسم: معرف_اسم
): اختيار معرف_رمز {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(الحالة.الروابط_التابعة) مثل غير_موقع)) {
        دع الربط: ربط_اسم_تابع = الحالة.الروابط_التابعة[ف];
        لو (معرفا_رمز_متساويان(الربط.المالك, المالك) &&
            معرفا_اسم_متساويان(الربط.الاسم, الاسم)) {
            أرجع اختيار.بعض(الربط.الرمز);
        };
        ف := ف + 1;
    }
    أرجع اختيار.لا_شيء();
}

عام عرف اسم_رمز_عند(
    سياق_الأسماء: &سياق_أسماء,
    المعرف: معرف_رمز
): اختيار معرف_اسم {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (المعرف.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحالة.الرموز[المعرف.القيمة].الاسم);
}

عام عرف نوع_رمز_عند(
    سياق_الأسماء: &سياق_أسماء,
    المعرف: معرف_رمز
): اختيار نوع_تصريح {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (المعرف.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحالة.الرموز[المعرف.القيمة].النوع);
}

عام عرف نطاق_تصريح_رمز_عند(
    سياق_الأسماء: &سياق_أسماء,
    المعرف: معرف_رمز
): اختيار نطاق {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (المعرف.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحالة.الرموز[المعرف.القيمة].نطاق_التصريح);
}

// Read-only scope queries keep scope storage owned by Name System.
عام عرف وحدة_نطاق_عند(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق
): اختيار معرف_وحدة {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحالة.النطاقات[النطاق.القيمة].الوحدة);
}

عام عرف عدد_رموز_نطاق(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق
): اختيار غير_موقع {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(
        الطول(الحالة.النطاقات[النطاق.القيمة].الرموز) مثل غير_موقع
    );
}

عام عرف رمز_نطاق_عند(
    سياق_الأسماء: &سياق_أسماء,
    النطاق: معرف_نطاق,
    الفهرس: غير_موقع
): اختيار معرف_رمز {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    لو (الفهرس >= (الطول(الحالة.النطاقات[النطاق.القيمة].الرموز) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحالة.النطاقات[النطاق.القيمة].الرموز[الفهرس]);
}

// Checked visibility accessor keeps symbol representation owned by Name System.
عام عرف علنية_رمز_عند(
    سياق_الأسماء: &سياق_أسماء,
    المعرف: معرف_رمز
): اختيار منطقي {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    لو (المعرف.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحالة.الرموز[المعرف.القيمة].علني);
}

// Primitive registry storage is global to one Name context, not to one source module.
// Source spelling ownership remains in primitive_registry.sn.
عام عرف ابحث_رمزا_بدائيا_بهوية(
    سياق_الأسماء: &سياق_أسماء,
    الاسم: معرف_اسم
): اختيار معرف_رمز {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(الحالة.أسماء_البدائيات) مثل غير_موقع)) {
        دع الربط: ربط_اسم_بدائي_مترجم = الحالة.أسماء_البدائيات[ف];
        لو (معرفا_اسم_متساويان(الربط.الاسم, الاسم)) {
            أرجع اختيار.بعض(الربط.الرمز);
        };
        ف := ف + 1;
    }
    أرجع اختيار.لا_شيء();
}

عام عرف أضف_رمزا_بدائيا(
    سياق_الأسماء: &متغير سياق_أسماء,
    النطاق_المالك: معرف_نطاق,
    التهجئة: نص,
    الهوية: هوية_بدائي_مترجم
): نتيجة_إضافة {
    دع الاسم: معرف_اسم = قنن_اسما_نصيا(سياق_الأسماء, &التهجئة);
    طابق ابحث_رمزا_بدائيا_بهوية(سياق_الأسماء, الاسم) مع
    | اختيار.بعض(الموجود) -> {
        أرجع نتيجة_إضافة.مكرر(الموجود, نطاق_فارغ());
    }
    | اختيار.لا_شيء -> { };

    دع الفارغ: نطاق = نطاق_فارغ();
    دع الرمز: معرف_رمز = خصص_رمزا(
        سياق_الأسماء,
        النطاق_المالك,
        الاسم,
        الفارغ,
        الفارغ,
        اختيار.لا_شيء(),
        نوع_تصريح.دالة_خارجية(),
        خطأ,
        خطأ
    );
    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    دع ربط_الاسم: ربط_اسم_بدائي_مترجم = { الاسم: الاسم, الرمز: الرمز };
    دع ربط_الهوية: ربط_رمز_بدائي_مترجم = { الرمز: الرمز, الهوية: الهوية };
    ألحق(الحالة.أسماء_البدائيات, ربط_الاسم);
    ألحق(الحالة.هويات_البدائيات, ربط_الهوية);
    *سياق_الأسماء := الحالة;
    أرجع نتيجة_إضافة.أضيف(الرمز);
}

عام عرف أضف_اسما_بدائيا(
    سياق_الأسماء: &متغير سياق_أسماء,
    التهجئة: نص,
    الرمز: معرف_رمز
): نتيجة_إضافة {
    دع الاسم: معرف_اسم = قنن_اسما_نصيا(سياق_الأسماء, &التهجئة);
    طابق ابحث_رمزا_بدائيا_بهوية(سياق_الأسماء, الاسم) مع
    | اختيار.بعض(الموجود) -> {
        أرجع نتيجة_إضافة.مكرر(الموجود, نطاق_فارغ());
    }
    | اختيار.لا_شيء -> { };

    دع الحالة: متغير سياق_أسماء = *سياق_الأسماء;
    لو (الرمز.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع نتيجة_إضافة.مكرر(الرمز, نطاق_فارغ());
    };
    دع ربط_الاسم: ربط_اسم_بدائي_مترجم = { الاسم: الاسم, الرمز: الرمز };
    ألحق(الحالة.أسماء_البدائيات, ربط_الاسم);
    *سياق_الأسماء := الحالة;
    أرجع نتيجة_إضافة.أضيف(الرمز);
}


// Canonical read-only Symbol -> Runtime-owned primitive identity query.
// Source spellings are confined to primitive_registry.sn registration. The
// compact binding table may outlive the larger Name context at phase boundaries.
عام عرف هوية_بدائي_في_روابط_رموز(
    الروابط: &متجه ربط_رمز_بدائي_مترجم,
    الرمز: معرف_رمز
): اختيار هوية_بدائي_مترجم {
    دع القيم: متجه ربط_رمز_بدائي_مترجم = *الروابط;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(القيم) مثل غير_موقع)) {
        دع الربط: ربط_رمز_بدائي_مترجم = القيم[ف];
        لو (معرفا_رمز_متساويان(الربط.الرمز, الرمز)) {
            أرجع اختيار.بعض(الربط.الهوية);
        };
        ف := ف + 1;
    }
    أرجع اختيار.لا_شيء();
}

عام عرف هوية_بدائي_رمز_عند(
    سياق_الأسماء: &سياق_أسماء,
    الرمز: معرف_رمز
): اختيار هوية_بدائي_مترجم {
    دع الحالة: سياق_أسماء = *سياق_الأسماء;
    أرجع هوية_بدائي_في_روابط_رموز(&الحالة.هويات_البدائيات, الرمز);
}

عام عرف ابن_سياق_استعلامات_أسماء(السياق: &سياق_أسماء): سياق_استعلامات_أسماء {
    دع الحالة: سياق_أسماء = *السياق;
    دع مرجع_النطاقات: &متجه نطاق_أسماء = &الحالة.النطاقات;
    دع النطاقات: متجه نطاق_أسماء = *مرجع_النطاقات;
    دع مرجع_الأسماء: &متجه اسم_مقنن = &الحالة.الأسماء;
    دع الأسماء: متجه اسم_مقنن = *مرجع_الأسماء;
    دع مرجع_الرموز: &متجه رمز_دلالي = &الحالة.الرموز;
    دع الرموز: متجه رمز_دلالي = *مرجع_الرموز;
    دع مرجع_الروابط_التابعة: &متجه ربط_اسم_تابع = &الحالة.الروابط_التابعة;
    دع الروابط_التابعة: متجه ربط_اسم_تابع = *مرجع_الروابط_التابعة;
    دع الناتج: سياق_استعلامات_أسماء = {
        النطاقات: النطاقات,
        الأسماء: الأسماء,
        الرموز: الرموز,
        الروابط_التابعة: الروابط_التابعة
    };
    أرجع الناتج;
}

عام عرف رمز_دلالي_عند_استعلام(
    السياق: &سياق_استعلامات_أسماء,
    الرمز: معرف_رمز
): اختيار رمز_دلالي {
    دع الحالة: سياق_استعلامات_أسماء = *السياق;
    لو (الرمز.القيمة >= (الطول(الحالة.الرموز) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    دع الحقيقة: رمز_دلالي = الحالة.الرموز[الرمز.القيمة];
    لو (!معرفا_رمز_متساويان(الحقيقة.المعرف, الرمز)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحقيقة);
}


عام عرف اسم_مقنن_عند_استعلام(
    السياق: &سياق_استعلامات_أسماء,
    الاسم: معرف_اسم
): اختيار اسم_مقنن {
    دع الحالة: سياق_استعلامات_أسماء = *السياق;
    لو (الاسم.القيمة >= (الطول(الحالة.الأسماء) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    دع مرجع_الحقيقة: &اسم_مقنن = &الحالة.الأسماء[الاسم.القيمة];
    دع الحقيقة: اسم_مقنن = *مرجع_الحقيقة;
    لو (!معرفا_اسم_متساويان(الحقيقة.المعرف, الاسم)) {
        أرجع اختيار.لا_شيء();
    };
    أرجع اختيار.بعض(الحقيقة);
}

عام عرف نطاق_دلالي_عند_استعلام(
    السياق: &سياق_استعلامات_أسماء,
    المصدر: غير_موقع,
    البايت: غير_موقع
): اختيار معرف_نطاق {
    دع الحالة: سياق_استعلامات_أسماء = *السياق;
    دع موجود: متغير منطقي = خطأ;
    دع الأفضل: متغير معرف_نطاق = معرف_نطاق_من(0);
    دع أفضل_طول: متغير غير_موقع = 0;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        دع مرجع_النطاق: &نطاق_أسماء = &الحالة.النطاقات[ف];
        دع النطاق: نطاق_أسماء = *مرجع_النطاق;
        طابق النطاق.نطاق_المصدر مع
        | اختيار.لا_شيء -> { }
        | اختيار.بعض(المدى) -> {
            لو (نطاق_يحتوي_بايتا(المدى, المصدر, البايت)) {
                دع الطول_الحالي: غير_موقع = طول_نطاق_بالبايت(المدى);
                لو ((!موجود) || (الطول_الحالي <= أفضل_طول)) {
                    موجود := صواب;
                    الأفضل := النطاق.المعرف;
                    أفضل_طول := الطول_الحالي;
                };
            };
        };
        ف := ف + 1;
    }
    لو (!موجود) { أرجع اختيار.لا_شيء(); };
    أرجع اختيار.بعض(الأفضل);
}

عرف قائمة_مرئية_تحوي_اسما(
    الروابط: &متجه ربط_اسم_نطاق,
    الاسم: معرف_اسم
): منطقي {
    دع القيم: متجه ربط_اسم_نطاق = *الروابط;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(القيم) مثل غير_موقع)) {
        لو (معرفا_اسم_متساويان(القيم[ف].الاسم, الاسم)) { أرجع صواب; };
        ف := ف + 1;
    }
    أرجع خطأ;
}

// Returns lexical bindings in resolution order: nearest scope first, with
// shadowed Name identities omitted. This is a general Name query, not LSP state.
عام عرف روابط_مرئية_عند_استعلام(
    السياق: &سياق_استعلامات_أسماء,
    المصدر: غير_موقع,
    البايت: غير_موقع
): متجه ربط_اسم_نطاق {
    دع الناتج: متغير متجه ربط_اسم_نطاق = [];
    دع الحالة: سياق_استعلامات_أسماء = *السياق;
    دع الحالي: متغير معرف_نطاق = معرف_نطاق_من(0);
    طابق نطاق_دلالي_عند_استعلام(السياق, المصدر, البايت) مع
    | اختيار.لا_شيء -> { أرجع الناتج; }
    | اختيار.بعض(المعرف) -> { الحالي := المعرف; };
    دع تابع: متغير منطقي = صواب;
    بينما (تابع) {
        لو (الحالي.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) { أرجع الناتج; };
        دع مرجع_النطاق: &نطاق_أسماء = &الحالة.النطاقات[الحالي.القيمة];
        دع النطاق: نطاق_أسماء = *مرجع_النطاق;
        دع ف: متغير غير_موقع = 0;
        بينما (ف < (الطول(النطاق.الروابط) مثل غير_موقع)) {
            دع الربط: ربط_اسم_نطاق = النطاق.الروابط[ف];
            لو (!قائمة_مرئية_تحوي_اسما(&الناتج, الربط.الاسم)) {
                ألحق(الناتج, الربط);
            };
            ف := ف + 1;
        }
        طابق النطاق.الأب مع
        | اختيار.لا_شيء -> { تابع := خطأ; }
        | اختيار.بعض(الأب) -> { الحالي := الأب; };
    }
    أرجع الناتج;
}

عام عرف اسم_مقنن_لنص_عند_استعلام(
    السياق: &سياق_استعلامات_أسماء,
    النص: &نص
): اختيار معرف_اسم {
    دع الحالة: سياق_استعلامات_أسماء = *السياق;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(الحالة.الأسماء) مثل غير_موقع)) {
        دع مرجع_الاسم: &اسم_مقنن = &الحالة.الأسماء[ف];
        دع الاسم: اسم_مقنن = *مرجع_الاسم;
        لو (تطابق_هوية_بايتات_ونص(&الاسم.البايتات, النص)) {
            أرجع اختيار.بعض(الاسم.المعرف);
        };
        ف := ف + 1;
    }
    أرجع اختيار.لا_شيء();
}

عام عرف ربط_محلي_عند_استعلام(
    السياق: &سياق_استعلامات_أسماء,
    النطاق: معرف_نطاق,
    الاسم: معرف_اسم
): اختيار معرف_رمز {
    دع الحالة: سياق_استعلامات_أسماء = *السياق;
    لو (النطاق.القيمة >= (الطول(الحالة.النطاقات) مثل غير_موقع)) {
        أرجع اختيار.لا_شيء();
    };
    دع مرجع_النطاق: &نطاق_أسماء = &الحالة.النطاقات[النطاق.القيمة];
    دع القيمة: نطاق_أسماء = *مرجع_النطاق;
    دع ف: متغير غير_موقع = 0;
    بينما (ف < (الطول(القيمة.الروابط) مثل غير_موقع)) {
        دع الربط: ربط_اسم_نطاق = القيمة.الروابط[ف];
        لو (معرفا_اسم_متساويان(الربط.الاسم, الاسم)) {
            أرجع اختيار.بعض(الربط.الرمز);
        };
        ف := ف + 1;
    }
    أرجع اختيار.لا_شيء();
}
