// GENERATED by tools/generate/generate_token_surface.py from token_registry.def.

استورد compiler.frontend.lexer.token;
استورد std.text.compare;

عام نوع مطابقة_عامل =
    | ليس_عاملا
    | عامل بنوع (رمز, عدد)
;

عام عرف العامل_عند(النص: &نص, الموضع: غير_موقع): مطابقة_عامل {
    لو (يطابق_عند(النص, الموضع, «>>>»)) {
        أرجع مطابقة_عامل.عامل(رمز.إزاحة_حسابية_يمنى(), 3);
    };
    لو (يطابق_عند(النص, الموضع, «<=»)) {
        أرجع مطابقة_عامل.عامل(رمز.أصغر_أو_يساوي(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «==»)) {
        أرجع مطابقة_عامل.عامل(رمز.يساوي(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «!=»)) {
        أرجع مطابقة_عامل.عامل(رمز.لا_يساوي(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «>=»)) {
        أرجع مطابقة_عامل.عامل(رمز.أكبر_أو_يساوي(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «&&»)) {
        أرجع مطابقة_عامل.عامل(رمز.و(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «||»)) {
        أرجع مطابقة_عامل.عامل(رمز.أو(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «<<»)) {
        أرجع مطابقة_عامل.عامل(رمز.إزاحة_يسرى(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «>>»)) {
        أرجع مطابقة_عامل.عامل(رمز.إزاحة_يمنى(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «:=»)) {
        أرجع مطابقة_عامل.عامل(رمز.إسناد(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «->»)) {
        أرجع مطابقة_عامل.عامل(رمز.سهم_يمين(), 2);
    };
    لو (يطابق_عند(النص, الموضع, «+»)) {
        أرجع مطابقة_عامل.عامل(رمز.جمع(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «-»)) {
        أرجع مطابقة_عامل.عامل(رمز.طرح(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «*»)) {
        أرجع مطابقة_عامل.عامل(رمز.نجمة(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «/»)) {
        أرجع مطابقة_عامل.عامل(رمز.قسمة(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «%»)) {
        أرجع مطابقة_عامل.عامل(رمز.باقي(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «=»)) {
        أرجع مطابقة_عامل.عامل(رمز.فاصل_تعريف(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «<»)) {
        أرجع مطابقة_عامل.عامل(رمز.أصغر(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «>»)) {
        أرجع مطابقة_عامل.عامل(رمز.أكبر(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «!»)) {
        أرجع مطابقة_عامل.عامل(رمز.نفي(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «&»)) {
        أرجع مطابقة_عامل.عامل(رمز.و_بتي(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «|»)) {
        أرجع مطابقة_عامل.عامل(رمز.أو_بتي(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «~»)) {
        أرجع مطابقة_عامل.عامل(رمز.متمم(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «{»)) {
        أرجع مطابقة_عامل.عامل(رمز.معقوف_يسار(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «}»)) {
        أرجع مطابقة_عامل.عامل(رمز.معقوف_يمين(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «^»)) {
        أرجع مطابقة_عامل.عامل(رمز.أو_حصري(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «.»)) {
        أرجع مطابقة_عامل.عامل(رمز.نقطة(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «,»)) {
        أرجع مطابقة_عامل.عامل(رمز.فاصلة(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «;»)) {
        أرجع مطابقة_عامل.عامل(رمز.فاصلة_منقوطة(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «:»)) {
        أرجع مطابقة_عامل.عامل(رمز.نقطتان(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «(»)) {
        أرجع مطابقة_عامل.عامل(رمز.قوس_يسار(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «)»)) {
        أرجع مطابقة_عامل.عامل(رمز.قوس_يمين(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «[»)) {
        أرجع مطابقة_عامل.عامل(رمز.قوس_مربع_يسار(), 1);
    };
    لو (يطابق_عند(النص, الموضع, «]»)) {
        أرجع مطابقة_عامل.عامل(رمز.قوس_مربع_يمين(), 1);
    };
    أرجع مطابقة_عامل.ليس_عاملا();
}
