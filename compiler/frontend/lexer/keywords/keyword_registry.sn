// GENERATED by tools/generate/generate_token_surface.py from token_registry.def.
// Spelling comes only from the token registry; semantic identity description is frontend-owned.

استورد compiler.frontend.lexer.token;
استورد compiler.frontend.identifier_identity;
استورد compiler.frontend.lexer.keywords.matching;

عام عرف كلمة_مسجلة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): مطابقة_كلمة {
    دع الوصف: وصف_هوية_معرف = صف_هوية_معرف_في_شريحة(النص, البداية, النهاية);
    لو (!الوصف.صالح || !الوصف.لها_محتوى) {
        أرجع مطابقة_كلمة.غير_محجوزة();
    };
    لو (الوصف.البايت_الأول == 97) {
        لو (الوصف.الطول == 2) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «as»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تحويل_نوع());
            };
        };
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «any»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.أي_نوع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «asm»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تجميع());
            };
        };
        لو (الوصف.الطول == 5) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «alias»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.مرادف_تصريح());
            };
        };
    };
    لو (الوصف.البايت_الأول == 98) {
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «box»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.صندوق());
            };
        };
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «bool»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.منطقي());
            };
        };
        لو (الوصف.الطول == 5) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «break»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.اخرج_من_حلقة());
            };
        };
    };
    لو (الوصف.البايت_الأول == 99) {
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «char»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.حرف());
            };
        };
        لو (الوصف.الطول == 8) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «continue»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تابع_حلقة());
            };
        };
    };
    لو (الوصف.البايت_الأول == 100) {
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «def»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تعريف_دالة());
            };
        };
    };
    لو (الوصف.البايت_الأول == 101) {
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «else»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.وإلا());
            };
        };
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «extern»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.خارجي());
            };
        };
    };
    لو (الوصف.البايت_الأول == 102) {
        لو (الوصف.الطول == 2) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «fn»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.نوع_دالة());
            };
        };
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «f32»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ع32());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «f64»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ع64());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «for»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.لكل());
            };
        };
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «from»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.من_مصدر());
            };
        };
        لو (الوصف.الطول == 5) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «float»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.عدد_عائم());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «false»)) {
                أرجع مطابقة_كلمة.منطقية(خطأ);
            };
        };
    };
    لو (الوصف.البايت_الأول == 105) {
        لو (الوصف.الطول == 2) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «if»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.لو());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «i8»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص8());
            };
        };
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «int»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.عدد());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «i16»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص16());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «i32»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص32());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «i64»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص64());
            };
        };
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «import»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.استيراد());
            };
        };
    };
    لو (الوصف.البايت_الأول == 108) {
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «let»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.دع());
            };
        };
    };
    لو (الوصف.البايت_الأول == 109) {
        لو (الوصف.الطول == 5) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «match»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.طابق());
            };
        };
        لو (الوصف.الطول == 7) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «mutable»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.متغير());
            };
        };
    };
    لو (الوصف.البايت_الأول == 111) {
        لو (الوصف.الطول == 2) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «of»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.بقيمة());
            };
        };
    };
    لو (الوصف.البايت_الأول == 112) {
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «public»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.عام());
            };
        };
    };
    لو (الوصف.البايت_الأول == 114) {
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «return»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.أرجع());
            };
        };
    };
    لو (الوصف.البايت_الأول == 115) {
        لو (الوصف.الطول == 5) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «slice»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.شريحة());
            };
        };
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «string»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.نص());
            };
        };
    };
    لو (الوصف.البايت_الأول == 116) {
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «type»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.نوع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «true»)) {
                أرجع مطابقة_كلمة.منطقية(صواب);
            };
        };
    };
    لو (الوصف.البايت_الأول == 117) {
        لو (الوصف.الطول == 2) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «u8»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ8());
            };
        };
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «use»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.استخدام());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «u16»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ16());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «u32»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ32());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «u64»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ64());
            };
        };
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «uint»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غير_موقع());
            };
        };
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «unsafe»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غير_آمن());
            };
        };
        لو (الوصف.الطول == 9) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «undefined»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غير_معرف());
            };
        };
    };
    لو (الوصف.البايت_الأول == 118) {
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «void»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.فراغ());
            };
        };
    };
    لو (الوصف.البايت_الأول == 119) {
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «with»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.مع());
            };
        };
        لو (الوصف.الطول == 5) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «while»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.بينما());
            };
        };
    };
    لو (الوصف.البايت_الأول == 216) {
        لو (الوصف.الطول == 3) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «ص8»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص8());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غ8»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ8());
            };
        };
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «دع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.دع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «أي»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.أي_نوع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «ص16»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص16());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «ص32»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص32());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «ص64»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ص64());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غ16»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ16());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غ32»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ32());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غ64»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غ64());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «ع32»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ع32());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «ع64»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.ع64());
            };
        };
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «عرف»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تعريف_دالة());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «حرف»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.حرف());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «عدد»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.عدد());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «عام»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.عام());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «خطأ»)) {
                أرجع مطابقة_كلمة.منطقية(خطأ);
            };
        };
        لو (الوصف.الطول == 8) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «دالة»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.نوع_دالة());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «بنوع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.بقيمة());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «أرجع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.أرجع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «طابق»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.طابق());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «صواب»)) {
                أرجع مطابقة_كلمة.منطقية(صواب);
            };
        };
        لو (الوصف.الطول == 10) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «خارجي»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.خارجي());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «تجميع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تجميع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «بينما»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.بينما());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «شريحة»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.شريحة());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «صندوق»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.صندوق());
            };
        };
        لو (الوصف.الطول == 12) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «استورد»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.استيراد());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «استخدم»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.استخدام());
            };
        };
        لو (الوصف.الطول == 13) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غير_آمن»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غير_آمن());
            };
        };
        لو (الوصف.الطول == 15) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غير_معرف»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غير_معرف());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «غير_موقع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.غير_موقع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «عدد_عائم»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.عدد_عائم());
            };
        };
        لو (الوصف.الطول == 17) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «تابع_حلقة»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تابع_حلقة());
            };
        };
        لو (الوصف.الطول == 22) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «اخرج_من_حلقة»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.اخرج_من_حلقة());
            };
        };
    };
    لو (الوصف.البايت_الأول == 217) {
        لو (الوصف.الطول == 4) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «من»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.من_مصدر());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «لو»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.لو());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «نص»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.نص());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «مع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.مع());
            };
        };
        لو (الوصف.الطول == 6) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «مثل»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.تحويل_نوع());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «لكل»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.لكل());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «نوع»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.نوع());
            };
        };
        لو (الوصف.الطول == 8) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «وإلا»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.وإلا());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «فراغ»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.فراغ());
            };
        };
        لو (الوصف.الطول == 10) {
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «مرادف»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.مرادف_تصريح());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «منطقي»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.منطقي());
            };
            لو (تطابق_هوية_معرف_وشريحة(النص, البداية, النهاية, «متغير»)) {
                أرجع مطابقة_كلمة.محجوزة(رمز.متغير());
            };
        };
    };
    أرجع مطابقة_كلمة.غير_محجوزة();
}
