// GENERATED by tools/generate/generate_identifier_policy.py from identifier_policy_registry.def.
// Do not edit identifier policy here; edit the canonical registry and regenerate.

استورد compiler.frontend.identifier_identity;

// سياسة معرفات SultanC صريحة: ASCII اللاتيني + العربية، لا "أي Unicode صالح".
عام عرف بداية_معرف(القيمة: غ32): منطقي {
    لو (((القيمة >= 0x0041) && (القيمة <= 0x005A))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0061) && (القيمة <= 0x007A))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0621) && (القيمة <= 0x063A))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0641) && (القيمة <= 0x064A))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x066E) && (القيمة <= 0x066F))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0671) && (القيمة <= 0x06D3))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06E5) && (القيمة <= 0x06E6))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06EE) && (القيمة <= 0x06EF))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06FA) && (القيمة <= 0x06FC))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0750) && (القيمة <= 0x077F))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x08A0) && (القيمة <= 0x08C9))) {
        أرجع صواب;
    };
    لو ((القيمة == 0x005F)) {
        أرجع صواب;
    };
    لو ((القيمة == 0x06D5)) {
        أرجع صواب;
    };
    لو ((القيمة == 0x06FF)) {
        أرجع صواب;
    };
    أرجع محرف_تهجئة_معرف(القيمة);
}

عام عرف تتمة_معرف(القيمة: غ32): منطقي {
    لو (بداية_معرف(القيمة)) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0030) && (القيمة <= 0x0039))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x064B) && (القيمة <= 0x065F))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x0660) && (القيمة <= 0x0669))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06D6) && (القيمة <= 0x06DC))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06DF) && (القيمة <= 0x06E4))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06E7) && (القيمة <= 0x06E8))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06EA) && (القيمة <= 0x06ED))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x06F0) && (القيمة <= 0x06F9))) {
        أرجع صواب;
    };
    لو (((القيمة >= 0x08D3) && (القيمة <= 0x08FF))) {
        أرجع صواب;
    };
    لو ((القيمة == 0x0670)) {
        أرجع صواب;
    };
    أرجع خطأ;
}
