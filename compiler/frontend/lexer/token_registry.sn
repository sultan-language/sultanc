// GENERATED by tools/generate/generate_token_surface.py from token_registry.def.
// Semantic classification is derived; consumers must not repeat token sets.

استورد compiler.frontend.lexer.token;

عام عرف هل_كلمة_محجوزة(القيمة: رمز): منطقي {
    طابق القيمة مع
    | رمز.نوع_دالة -> {
        أرجع صواب;
    }
    | رمز.بقيمة -> {
        أرجع صواب;
    }
    | رمز.من_مصدر -> {
        أرجع صواب;
    }
    | رمز.خارجي -> {
        أرجع صواب;
    }
    | رمز.استيراد -> {
        أرجع صواب;
    }
    | رمز.استخدام -> {
        أرجع صواب;
    }
    | رمز.تحويل_نوع -> {
        أرجع صواب;
    }
    | رمز.غير_معرف -> {
        أرجع صواب;
    }
    | رمز.تعريف_دالة -> {
        أرجع صواب;
    }
    | رمز.مرادف_تصريح -> {
        أرجع صواب;
    }
    | رمز.دع -> {
        أرجع صواب;
    }
    | رمز.أرجع -> {
        أرجع صواب;
    }
    | رمز.وإلا -> {
        أرجع صواب;
    }
    | رمز.لو -> {
        أرجع صواب;
    }
    | رمز.أي_نوع -> {
        أرجع صواب;
    }
    | رمز.منطقي -> {
        أرجع صواب;
    }
    | رمز.نص -> {
        أرجع صواب;
    }
    | رمز.حرف -> {
        أرجع صواب;
    }
    | رمز.عدد -> {
        أرجع صواب;
    }
    | رمز.غير_موقع -> {
        أرجع صواب;
    }
    | رمز.عدد_عائم -> {
        أرجع صواب;
    }
    | رمز.فراغ -> {
        أرجع صواب;
    }
    | رمز.ص8 -> {
        أرجع صواب;
    }
    | رمز.ص16 -> {
        أرجع صواب;
    }
    | رمز.ص32 -> {
        أرجع صواب;
    }
    | رمز.ص64 -> {
        أرجع صواب;
    }
    | رمز.غ8 -> {
        أرجع صواب;
    }
    | رمز.غ16 -> {
        أرجع صواب;
    }
    | رمز.غ32 -> {
        أرجع صواب;
    }
    | رمز.غ64 -> {
        أرجع صواب;
    }
    | رمز.ع32 -> {
        أرجع صواب;
    }
    | رمز.ع64 -> {
        أرجع صواب;
    }
    | رمز.تجميع -> {
        أرجع صواب;
    }
    | رمز.غير_آمن -> {
        أرجع صواب;
    }
    | رمز.بينما -> {
        أرجع صواب;
    }
    | رمز.لكل -> {
        أرجع صواب;
    }
    | رمز.اخرج_من_حلقة -> {
        أرجع صواب;
    }
    | رمز.تابع_حلقة -> {
        أرجع صواب;
    }
    | رمز.نوع -> {
        أرجع صواب;
    }
    | رمز.طابق -> {
        أرجع صواب;
    }
    | رمز.مع -> {
        أرجع صواب;
    }
    | رمز.متغير -> {
        أرجع صواب;
    }
    | رمز.عام -> {
        أرجع صواب;
    }
    | رمز.شريحة -> {
        أرجع صواب;
    }
    | رمز.صندوق -> {
        أرجع صواب;
    }
    | رمز.حرفي_منطقي -> {
        أرجع صواب;
    }
    | _ -> {
        أرجع خطأ;
    };
}

عام عرف هل_رمز_مستقبلي(القيمة: رمز): منطقي {
    طابق القيمة مع
    | رمز.استخدام -> {
        أرجع صواب;
    }
    | رمز.عدد_عائم -> {
        أرجع صواب;
    }
    | رمز.شرطة_سفلية_مرقمة -> {
        أرجع صواب;
    }
    | _ -> {
        أرجع خطأ;
    };
}
