// GENERATED by tools/generate/generate_identifier_policy.py from identifier_policy_registry.def.
// Do not edit identifier policy here; edit the canonical registry and regenerate.

استورد std.encoding.utf8;
استورد std.bytes.compare;

// الهوية الدلالية لمعرف SultanC تزيل محارف التهجئة المسجلة وتطبع المحارف المسجلة فقط.
// لا تطبق هذه الطبقة ترجمة بين العربية والإنجليزية.
عام عرف محرف_تهجئة_معرف(القيمة: غ32): منطقي {
    لو ((القيمة == 0x0640)) {
        أرجع صواب;
    };
    أرجع خطأ;
}

عام عرف طبع_محرف_هوية_معرف(القيمة: غ32): غ32 {
    لو (القيمة == 0x0622) { أرجع 0x0627; };
    لو (القيمة == 0x0623) { أرجع 0x0627; };
    لو (القيمة == 0x0625) { أرجع 0x0627; };
    أرجع القيمة;
}

عام نوع وصف_هوية_معرف = {
    صالح: منطقي,
    لها_محتوى: منطقي,
    الطول: غير_موقع,
    البايت_الأول: غ8
};

عام عرف صف_هوية_معرف_في_شريحة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): وصف_هوية_معرف {
    دع غير_صالح: وصف_هوية_معرف = {
        صالح: خطأ,
        لها_محتوى: خطأ,
        الطول: 0,
        البايت_الأول: 0
    };
    لو (النهاية < البداية) {
        أرجع غير_صالح;
    };
    دع الأصل: نص = *النص;
    دع الموضع: متغير غير_موقع = البداية;
    دع العدد: متغير غير_موقع = 0;
    دع الأول: متغير غ8 = 0;
    دع له_محتوى: متغير منطقي = خطأ;
    بينما (الموضع < النهاية) {
        دع المفكوك: فك_يوتيف8 = فك_محرف_يوتيف8(&الأصل, الموضع);
        لو (!المفكوك.صالح || ((الموضع + المفكوك.العرض) > النهاية)) {
            أرجع غير_صالح;
        };
        لو (!محرف_تهجئة_معرف(المفكوك.القيمة)) {
            لو (!له_محتوى) {
                الأول := الأصل[الموضع];
                له_محتوى := صواب;
            };
            العدد := العدد + المفكوك.العرض;
        };
        الموضع := الموضع + المفكوك.العرض;
    }
    دع الناتج: وصف_هوية_معرف = {
        صالح: صواب,
        لها_محتوى: له_محتوى,
        الطول: العدد,
        البايت_الأول: الأول
    };
    أرجع الناتج;
}

عام عرف طول_هوية_معرف_في_شريحة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): غير_موقع {
    دع الوصف: وصف_هوية_معرف = صف_هوية_معرف_في_شريحة(النص, البداية, النهاية);
    لو (!الوصف.صالح) {
        أرجع 0;
    };
    أرجع الوصف.الطول;
}

عام عرف هوية_معرف_لها_محتوى_في_شريحة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): منطقي {
    دع الوصف: وصف_هوية_معرف = صف_هوية_معرف_في_شريحة(النص, البداية, النهاية);
    أرجع الوصف.صالح && الوصف.لها_محتوى;
}

عام عرف تطابق_هوية_معرف_وشريحة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع,
    المطلوب: نص
): منطقي {
    لو (النهاية < البداية) {
        أرجع خطأ;
    };
    دع الأصل_المطلوب: نص = المطلوب;
    دع حد_المطلوب: غير_موقع = الطول(الأصل_المطلوب) مثل غير_موقع;
    دع أ: متغير غير_موقع = البداية;
    دع ب: متغير غير_موقع = 0;
    بينما (صواب) {
        دع تخط_أ: متغير منطقي = صواب;
        بينما (تخط_أ && (أ < النهاية)) {
            دع مفكوك_أ: فك_يوتيف8 = فك_محرف_يوتيف8(النص, أ);
            لو (!مفكوك_أ.صالح || ((أ + مفكوك_أ.العرض) > النهاية)) {
                أرجع خطأ;
            };
            لو (محرف_تهجئة_معرف(مفكوك_أ.القيمة)) {
                أ := أ + مفكوك_أ.العرض;
            }
            وإلا {
                تخط_أ := خطأ;
            };
        }

        دع تخط_ب: متغير منطقي = صواب;
        بينما (تخط_ب && (ب < حد_المطلوب)) {
            دع مفكوك_ب: فك_يوتيف8 = فك_محرف_يوتيف8(&الأصل_المطلوب, ب);
            لو (!مفكوك_ب.صالح || ((ب + مفكوك_ب.العرض) > حد_المطلوب)) {
                أرجع خطأ;
            };
            لو (محرف_تهجئة_معرف(مفكوك_ب.القيمة)) {
                ب := ب + مفكوك_ب.العرض;
            }
            وإلا {
                تخط_ب := خطأ;
            };
        }

        لو ((أ >= النهاية) || (ب >= حد_المطلوب)) {
            أرجع (أ >= النهاية) && (ب >= حد_المطلوب);
        };

        دع قيمة_أ: فك_يوتيف8 = فك_محرف_يوتيف8(النص, أ);
        دع قيمة_ب: فك_يوتيف8 = فك_محرف_يوتيف8(&الأصل_المطلوب, ب);
        لو (!قيمة_أ.صالح || !قيمة_ب.صالح) {
            أرجع خطأ;
        };
        لو ((أ + قيمة_أ.العرض) > النهاية) {
            أرجع خطأ;
        };
        لو (طبع_محرف_هوية_معرف(قيمة_أ.القيمة) != طبع_محرف_هوية_معرف(قيمة_ب.القيمة)) {
            أرجع خطأ;
        };
        أ := أ + قيمة_أ.العرض;
        ب := ب + قيمة_ب.العرض;
    }
    أرجع صواب;
}

عام عرف انسخ_هوية_معرف_من_شريحة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): متجه غ8 {
    دع الأصل: نص = *النص;
    دع الناتج: متغير متجه غ8 = [];
    دع الموضع: متغير غير_موقع = البداية;
    بينما (الموضع < النهاية) {
        دع المفكوك: فك_يوتيف8 = فك_محرف_يوتيف8(&الأصل, الموضع);
        لو (!المفكوك.صالح || ((الموضع + المفكوك.العرض) > النهاية)) {
            أرجع الناتج;
        };
        لو (!محرف_تهجئة_معرف(المفكوك.القيمة)) {
            دع المطبّع: غ32 = طبع_محرف_هوية_معرف(المفكوك.القيمة);
            طابق ألحق_محرف_يوتيف8(الناتج, المطبّع) مع
            | نتيجة_ترميز_يوتيف8.مشفر(الجديدة) -> {
                الناتج := الجديدة;
            }
            | نتيجة_ترميز_يوتيف8.قيمة_غير_صالحة -> {
                دع الفارغ: متجه غ8 = [];
                أرجع الفارغ;
            };
        };
        الموضع := الموضع + المفكوك.العرض;
    }
    أرجع الناتج;
}

عام عرف انسخ_هوية_معرف_من_نص(النص: &نص): متجه غ8 {
    دع الأصل: نص = *النص;
    دع النهاية: غير_موقع = الطول(الأصل) مثل غير_موقع;
    أرجع انسخ_هوية_معرف_من_شريحة(&الأصل, 0, النهاية);
}

عام عرف هويتان_معرفيتان_متساويتان(
    الأولى: &متجه غ8,
    الثانية: &متجه غ8
): منطقي {
    أرجع متجها_بايتات_متساويان(الأولى, الثانية);
}

// Stable bounded hash for canonical identifier identity. Hashing operates on
// canonical Unicode scalars so normalized source spellings share the same bucket.
عام عرف هاش_هوية_معرف_بايتات(الهوية: &متجه غ8): غير_موقع {
    دع البايتات: متجه غ8 = *الهوية;
    دع الهاش: متغير غير_موقع = 0;
    دع الموضع: متغير غير_موقع = 0;
    بينما (الموضع < (الطول(البايتات) مثل غير_موقع)) {
        دع المفكوك: فك_يوتيف8 = فك_محرف_يوتيف8_من_بايتات(&البايتات, الموضع);
        لو (!المفكوك.صالح) { أرجع 0; };
        دع القيمة: غير_موقع = طبع_محرف_هوية_معرف(المفكوك.القيمة) مثل غير_موقع;
        الهاش := ((الهاش * 257) + القيمة) % 1048573;
        الموضع := الموضع + المفكوك.العرض;
    }
    أرجع الهاش;
}

// Hash source identity without allocating its canonical byte vector. Spelling
// characters are skipped and registered scalar variants are normalized.
عام عرف هاش_هوية_معرف_في_شريحة(
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): غير_موقع {
    دع الأصل: نص = *النص;
    دع الموضع: متغير غير_موقع = البداية;
    دع الهاش: متغير غير_موقع = 0;
    بينما (الموضع < النهاية) {
        دع المفكوك: فك_يوتيف8 = فك_محرف_يوتيف8(&الأصل, الموضع);
        لو (!المفكوك.صالح || ((الموضع + المفكوك.العرض) > النهاية)) {
            أرجع 0;
        };
        لو (!محرف_تهجئة_معرف(المفكوك.القيمة)) {
            دع القيمة: غير_موقع = طبع_محرف_هوية_معرف(المفكوك.القيمة) مثل غير_موقع;
            الهاش := ((الهاش * 257) + القيمة) % 1048573;
        };
        الموضع := الموضع + المفكوك.العرض;
    }
    أرجع الهاش;
}

عام عرف هاش_هوية_معرف_في_نص(النص: &نص): غير_موقع {
    دع الأصل: نص = *النص;
    دع النهاية: غير_موقع = الطول(الأصل) مثل غير_موقع;
    أرجع هاش_هوية_معرف_في_شريحة(&الأصل, 0, النهاية);
}

// Compare a canonical identity vector directly with a source slice. Both sides
// are decoded as scalars so registered normalization remains allocation-free.
عام عرف تطابق_هوية_بايتات_وشريحة(
    الهوية: &متجه غ8,
    النص: &نص,
    البداية: غير_موقع,
    النهاية: غير_موقع
): منطقي {
    دع المطلوب: متجه غ8 = *الهوية;
    دع الأصل: نص = *النص;
    دع موضع_المصدر: متغير غير_موقع = البداية;
    دع موضع_الهوية: متغير غير_موقع = 0;
    بينما (موضع_المصدر < النهاية) {
        دع مفكوك_المصدر: فك_يوتيف8 = فك_محرف_يوتيف8(&الأصل, موضع_المصدر);
        لو (!مفكوك_المصدر.صالح || ((موضع_المصدر + مفكوك_المصدر.العرض) > النهاية)) {
            أرجع خطأ;
        };
        موضع_المصدر := موضع_المصدر + مفكوك_المصدر.العرض;
        لو (!محرف_تهجئة_معرف(مفكوك_المصدر.القيمة)) {
            لو (موضع_الهوية >= (الطول(المطلوب) مثل غير_موقع)) { أرجع خطأ; };
            دع مفكوك_الهوية: فك_يوتيف8 = فك_محرف_يوتيف8_من_بايتات(&المطلوب, موضع_الهوية);
            لو (!مفكوك_الهوية.صالح) { أرجع خطأ; };
            لو (
                طبع_محرف_هوية_معرف(مفكوك_المصدر.القيمة)
                != طبع_محرف_هوية_معرف(مفكوك_الهوية.القيمة)
            ) {
                أرجع خطأ;
            };
            موضع_الهوية := موضع_الهوية + مفكوك_الهوية.العرض;
        };
    }
    أرجع موضع_الهوية == (الطول(المطلوب) مثل غير_موقع);
}

عام عرف تطابق_هوية_بايتات_ونص(الهوية: &متجه غ8, النص: &نص): منطقي {
    دع الأصل: نص = *النص;
    دع النهاية: غير_موقع = الطول(الأصل) مثل غير_موقع;
    أرجع تطابق_هوية_بايتات_وشريحة(الهوية, &الأصل, 0, النهاية);
}
