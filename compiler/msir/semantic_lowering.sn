استورد compiler.frontend.semantic_pipeline;
استورد compiler.msir.msir_model;
استورد compiler.msir.identifiers;
استورد compiler.diagnostics.diagnostic_model;
استورد compiler.msir.lowering.model;
استورد compiler.msir.lowering.functions;
استورد compiler.msir.lowering.monomorphization;
استورد compiler.msir.lowering.diagnostics;
استورد compiler.frontend.ast.unit;
استورد compiler.frontend.ast.name;
استورد compiler.frontend.types.type_context;
استورد compiler.frontend.types.semantic_pipeline;
استورد compiler.frontend.types.expression_types;
استورد compiler.frontend.names.context;
استورد compiler.frontend.names.resolved_bindings;
استورد compiler.frontend.pattern.pattern_registry;
استورد compiler.frontend.pattern.type_validation;
استورد compiler.msir.msir_builder;
استورد compiler.frontend.source.source;
استورد compiler.frontend.source.position;
استورد compiler.frontend.ast.declarations;
استورد compiler.frontend.ast.statements;
استورد compiler.frontend.ast.literals;
استورد compiler.frontend.ast.types;
استورد compiler.frontend.pattern.pattern_model;
استورد compiler.frontend.pattern.pattern_construction;
استورد compiler.frontend.source.scope;
استورد compiler.frontend.types.type_identity;
استورد compiler.frontend.types.type_model;
استورد compiler.frontend.types.type_registry;
استورد compiler.frontend.types.type_semantic_shape;
استورد compiler.frontend.types.type_semantic_shape_queries;
استورد compiler.frontend.types.compatibility;
استورد compiler.frontend.types.builtins_generated;
استورد compiler.frontend.names.identifiers;
استورد compiler.msir.lowering.context;
استورد compiler.msir.lowering.statements;

// Generic functions remain semantic templates until MSIR requests concrete instances.
عرف أعلن_مثيلات_دوال_عامة_للخفض(
    البرنامج: &متغير برنامج_مسير,
    الوحدات: &متجه وحدة_مصدر,
    الأنواع: &متغير سجل_أنواع,
    أنواع_الرموز: &سياق_أنواع_رموز,
    روابط_الأسماء: &متجه سياق_روابط_أسماء,
    الطلبات: &متجه طلب_مثيل_دالة_خفض,
    دوال_الخفض: &متغير سياق_دوال_خفض
): اختيار خطأ_خفض_مسير {
    دع القيم: متجه وحدة_مصدر = *الوحدات;
    دع الروابط: متجه سياق_روابط_أسماء = *روابط_الأسماء;
    دع قائمة_الطلبات: متجه طلب_مثيل_دالة_خفض = *الطلبات;
    دع ط: متغير غير_موقع = 0;
    بينما (ط < (الطول(قائمة_الطلبات) مثل غير_موقع)) {
        دع رمز_الطلب: معرف_رمز = قائمة_الطلبات[ط].الرمز;
        دع و: متغير غير_موقع = 0;
        بينما (و < (الطول(القيم) مثل غير_موقع)) {
            دع مرجع_الوحدة: &وحدة_مصدر = &القيم[و];
            دع الوحدة: وحدة_مصدر = *مرجع_الوحدة;
            دع ف: متغير غير_موقع = 0;
            بينما (ف < (الطول(الوحدة.العناصر) مثل غير_موقع)) {
                دع مرجع_العنصر: &عنصر_وحدة = &الوحدة.العناصر[ف];
                دع العنصر: عنصر_وحدة = *مرجع_العنصر;
                طابق العنصر مع
                | عنصر_وحدة.دالة(الدالة) -> {
                    لو (الطول(الدالة.معاملات_النوع) > 0) {
                        طابق رمز_اسم_محلول_عند(&الروابط[و], الدالة.الاسم.النطاق) مع
                        | اختيار.بعض(الرمز) -> {
                            لو (معرفا_رمز_متساويان(الرمز, رمز_الطلب)) {
                                طابق أعلن_دالة_للخفض(
                                    البرنامج, &الوحدة.المصدر, &الدالة, الأنواع, أنواع_الرموز, &الروابط[و],
                                    &قائمة_الطلبات[ط].استبدالات_النوع, دوال_الخفض
                                ) مع
                                | اختيار.بعض(الخطأ) -> { أرجع اختيار.بعض(الخطأ); }
                                | اختيار.لا_شيء -> { };
                            };
                        }
                        | اختيار.لا_شيء -> { };
                    };
                }
                | _ -> { };
                ف := ف + 1;
            }
            و := و + 1;
        }
        ط := ط + 1;
    }
    أرجع اختيار.لا_شيء();
}

عرف اخفض_مثيلات_دوال_عامة_إلى_مسير(
    البرنامج: &متغير برنامج_مسير,
    الوحدات: &متجه وحدة_مصدر,
    الأنواع: &متغير سجل_أنواع,
    أنواع_الرموز: &سياق_أنواع_رموز,
    أنواع_التعبيرات: &متجه سياق_أنواع_تعبيرات,
    روابط_الأسماء: &متجه سياق_روابط_أسماء,
    سجل_الأنماط: &سجل_أنماط,
    تحققات_الأنماط: &متجه ربط_تحقق_نمط_وحدة,
    دوال_الخفض: &سياق_دوال_خفض,
    هويات_البدائيات: &متجه ربط_رمز_بدائي_مترجم,
    الطلبات: &متجه طلب_مثيل_دالة_خفض
): اختيار خطأ_خفض_مسير {
    دع القيم: متجه وحدة_مصدر = *الوحدات;
    دع الروابط: متجه سياق_روابط_أسماء = *روابط_الأسماء;
    دع سياقات_التعبيرات: متجه سياق_أنواع_تعبيرات = *أنواع_التعبيرات;
    دع قائمة_الطلبات: متجه طلب_مثيل_دالة_خفض = *الطلبات;
    دع ط: متغير غير_موقع = 0;
    بينما (ط < (الطول(قائمة_الطلبات) مثل غير_موقع)) {
        دع رمز_الطلب: معرف_رمز = قائمة_الطلبات[ط].الرمز;
        دع و: متغير غير_موقع = 0;
        بينما (و < (الطول(القيم) مثل غير_موقع)) {
            دع مرجع_الوحدة: &وحدة_مصدر = &القيم[و];
            دع الوحدة: وحدة_مصدر = *مرجع_الوحدة;
            دع ف: متغير غير_موقع = 0;
            بينما (ف < (الطول(الوحدة.العناصر) مثل غير_موقع)) {
                دع مرجع_العنصر: &عنصر_وحدة = &الوحدة.العناصر[ف];
                دع العنصر: عنصر_وحدة = *مرجع_العنصر;
                طابق العنصر مع
                | عنصر_وحدة.دالة(الدالة) -> {
                    لو (الطول(الدالة.معاملات_النوع) > 0) {
                        طابق رمز_اسم_محلول_عند(&الروابط[و], الدالة.الاسم.النطاق) مع
                        | اختيار.بعض(الرمز) -> {
                            لو (معرفا_رمز_متساويان(الرمز, رمز_الطلب)) {
                                دع أنواع_رموز_المثيل: سياق_أنواع_رموز = خصص_أنواع_رموز_للمثيل(
                                    الأنواع, أنواع_الرموز, &قائمة_الطلبات[ط].استبدالات_النوع
                                );
                                دع أنواع_تعبيرات_المثيل: سياق_أنواع_تعبيرات = خصص_أنواع_تعبيرات_للمثيل(
                                    الأنواع, &سياقات_التعبيرات[و], &قائمة_الطلبات[ط].استبدالات_النوع
                                );
                                دع تحققات_المثيل: متجه ربط_تحقق_نمط_وحدة = خصص_تحققات_أنماط_للمثيل(
                                    الأنواع, تحققات_الأنماط, &قائمة_الطلبات[ط].استبدالات_النوع
                                );
                                طابق اخفض_جسم_دالة_إلى_مسير(
                                    البرنامج, &الدالة, الأنواع, &أنواع_رموز_المثيل,
                                    &أنواع_تعبيرات_المثيل, &الروابط[و],
                                    &قائمة_الطلبات[ط].استبدالات_النوع, سجل_الأنماط,
                                    &تحققات_المثيل, دوال_الخفض, هويات_البدائيات
                                ) مع
                                | اختيار.بعض(الخطأ) -> { أرجع اختيار.بعض(الخطأ); }
                                | اختيار.لا_شيء -> { };
                            };
                        }
                        | اختيار.لا_شيء -> { };
                    };
                }
                | _ -> { };
                ف := ف + 1;
            }
            و := و + 1;
        }
        ط := ط + 1;
    }
    أرجع اختيار.لا_شيء();
}

// Whole-unit assembly is private to the canonical semantic lowering boundary.
عرف اخفض_وحدات_دلالية_إلى_مسير(
    الوحدات: &متجه وحدة_مصدر,
    الأنواع: &متغير سجل_أنواع,
    أنواع_الرموز: &سياق_أنواع_رموز,
    أنواع_التعبيرات: &متجه سياق_أنواع_تعبيرات,
    روابط_الأسماء: &متجه سياق_روابط_أسماء,
    سجل_الأنماط: &سجل_أنماط,
    تحققات_الأنماط: &متجه ربط_تحقق_نمط_وحدة,
    هويات_البدائيات: &متجه ربط_رمز_بدائي_مترجم,
    رمز_بدء_البرنامج: اختيار معرف_رمز
): نتيجة_خفض_مسير {
    دع القيم: متجه وحدة_مصدر = *الوحدات;
    دع روابط_القيم: متجه سياق_روابط_أسماء = *روابط_الأسماء;
    دع أنواع_تعبيرات_القيم: متجه سياق_أنواع_تعبيرات = *أنواع_التعبيرات;
    دع البرنامج: متغير برنامج_مسير = برنامج_مسير_جديد();
    دع دوال_الخفض: متغير سياق_دوال_خفض = سياق_دوال_خفض_جديد();
    دع بلا_استبدالات: متجه ربط_استبدال_نوع = [];
    دع طلبات_المثيلات: متجه طلب_مثيل_دالة_خفض = ابن_طلبات_مثيلات_دوال(
        الوحدات, الأنواع, أنواع_التعبيرات, روابط_الأسماء
    );

    // Declare every semantic function before lowering any body. Cross-module calls therefore
    // consume the same canonical Symbol -> FunctionID map and are independent of module order.
    دع و: متغير غير_موقع = 0;
    بينما (و < (الطول(القيم) مثل غير_موقع)) {
        دع مرجع_الوحدة: &وحدة_مصدر = &القيم[و];
        دع روابط_الوحدة: &سياق_روابط_أسماء = &روابط_القيم[و];
        دع الأصل: وحدة_مصدر = *مرجع_الوحدة;
        دع ف: متغير غير_موقع = 0;
        بينما (ف < (الطول(الأصل.العناصر) مثل غير_موقع)) {
            دع مرجع_العنصر: &عنصر_وحدة = &الأصل.العناصر[ف];
            دع العنصر: عنصر_وحدة = *مرجع_العنصر;
            طابق العنصر مع
            | عنصر_وحدة.دالة(الدالة) -> {
                لو (الطول(الدالة.معاملات_النوع) == 0) {
                    طابق أعلن_دالة_للخفض(
                        &متغير البرنامج, &الأصل.المصدر, &الدالة, الأنواع, أنواع_الرموز,
                        روابط_الوحدة, &بلا_استبدالات, &متغير دوال_الخفض
                    ) مع
                    | اختيار.بعض(الخطأ) -> { أرجع نتيجة_خفض_مسير.مرفوض(الخطأ); }
                    | اختيار.لا_شيء -> { };
                };
            }
            | عنصر_وحدة.نوع(_) -> { }
            | عنصر_وحدة.تجميع(_, _) -> { };
            ف := ف + 1;
        }
        و := و + 1;
    }

    طابق أعلن_مثيلات_دوال_عامة_للخفض(
        &متغير البرنامج, الوحدات, الأنواع, أنواع_الرموز, روابط_الأسماء,
        &طلبات_المثيلات, &متغير دوال_الخفض
    ) مع
    | اختيار.بعض(الخطأ) -> { أرجع نتيجة_خفض_مسير.مرفوض(الخطأ); }
    | اختيار.لا_شيء -> { };

    طابق رمز_بدء_البرنامج مع
    | اختيار.بعض(رمز_البدء) -> {
        طابق دالة_خفض_عند(&دوال_الخفض, رمز_البدء) مع
        | اختيار.بعض(ربط_البدء) -> {
            طابق ربط_البدء.الهدف مع
            | هدف_دالة_خفض.داخلية(معرف_البدء) -> {
                طابق عين_دالة_بدء_برنامج_مسير(&متغير البرنامج, معرف_البدء) مع
                | اختيار.بعض(_) -> {
                    أرجع نتيجة_خفض_مسير.مرفوض(خطأ_خفض_مسير.دالة_دلالية_مفقودة(رمز_البدء));
                }
                | اختيار.لا_شيء -> { };
            }
            | هدف_دالة_خفض.خارجية(_) -> {
                أرجع نتيجة_خفض_مسير.مرفوض(خطأ_خفض_مسير.دالة_دلالية_مفقودة(رمز_البدء));
            };
        }
        | اختيار.لا_شيء -> {
            أرجع نتيجة_خفض_مسير.مرفوض(خطأ_خفض_مسير.دالة_دلالية_مفقودة(رمز_البدء));
        };
    }
    | اختيار.لا_شيء -> { };

    و := 0;
    بينما (و < (الطول(القيم) مثل غير_موقع)) {
        دع مرجع_الوحدة: &وحدة_مصدر = &القيم[و];
        دع روابط_الوحدة: &سياق_روابط_أسماء = &روابط_القيم[و];
        دع أنواع_تعبيرات_الوحدة: &سياق_أنواع_تعبيرات = &أنواع_تعبيرات_القيم[و];
        دع الأصل: وحدة_مصدر = *مرجع_الوحدة;
        دع ف: متغير غير_موقع = 0;
        بينما (ف < (الطول(الأصل.العناصر) مثل غير_موقع)) {
            دع مرجع_العنصر: &عنصر_وحدة = &الأصل.العناصر[ف];
            دع العنصر: عنصر_وحدة = *مرجع_العنصر;
            طابق العنصر مع
            | عنصر_وحدة.دالة(الدالة) -> {
                لو (الطول(الدالة.معاملات_النوع) == 0) {
                    طابق اخفض_جسم_دالة_إلى_مسير(
                        &متغير البرنامج, &الدالة, الأنواع, أنواع_الرموز, أنواع_تعبيرات_الوحدة,
                        روابط_الوحدة, &بلا_استبدالات, سجل_الأنماط, تحققات_الأنماط,
                        &دوال_الخفض, هويات_البدائيات
                    ) مع
                    | اختيار.بعض(الخطأ) -> { أرجع نتيجة_خفض_مسير.مرفوض(الخطأ); }
                    | اختيار.لا_شيء -> { };
                };
            }
            | _ -> { };
            ف := ف + 1;
        }
        و := و + 1;
    }

    طابق اخفض_مثيلات_دوال_عامة_إلى_مسير(
        &متغير البرنامج, الوحدات, الأنواع, أنواع_الرموز, أنواع_التعبيرات,
        روابط_الأسماء, سجل_الأنماط, تحققات_الأنماط, &دوال_الخفض,
        هويات_البدائيات, &طلبات_المثيلات
    ) مع
    | اختيار.بعض(الخطأ) -> { أرجع نتيجة_خفض_مسير.مرفوض(الخطأ); }
    | اختيار.لا_شيء -> { };

    // Builder/lowering own construction invariants. Production lowering must not
    // rescan the complete MSIR program with dominance/definition verification.
    أرجع نتيجة_خفض_مسير.ناجح(البرنامج);
}

// الحد العام الوحيد من Frontend المقنن إلى MSIR المتحقق.
// The public MSIR boundary exposes canonical diagnostics, never MSIR-internal failures.
عام نوع نتيجة_خفض_واجهة_مسير =
    | ناجح بنوع (برنامج_مسير, سجل_أنواع)
    | مرفوض بنوع مجموعة_تشخيصات
;

// Public Frontend -> MSIR handoff. Driver passes the canonical whole-unit semantic result
// without unpacking owner-specific contexts. MSIR consumes the semantic facts internally.
عام عرف اخفض_واجهة_وحدة_إلى_مسير(
    الواجهة: متغير نتيجة_واجهة_وحدة
): نتيجة_خفض_واجهة_مسير {
    // Lower directly from the owned Frontend result. The previous boundary copied
    // every large semantic field into a second aggregate before lowering, keeping
    // both copies alive at once. Borrow the canonical fields in place instead.
    طابق اخفض_وحدات_دلالية_إلى_مسير(
        &الواجهة.الوحدات,
        &متغير الواجهة.الأنواع,
        &الواجهة.أنواع_الرموز,
        &الواجهة.أنواع_التعبيرات,
        &الواجهة.روابط_الأسماء,
        &الواجهة.الأنماط,
        &الواجهة.تحققات_الأنماط,
        &الواجهة.هويات_البدائيات,
        الواجهة.رمز_بدء_البرنامج
    ) مع
    | نتيجة_خفض_مسير.ناجح(البرنامج) -> {
        // Lowering is the final consumer of AST/Name/Pattern/Memory/SSF state.
        // Move only the Type registry forward with MSIR; every other Frontend-owned
        // structure dies with `الواجهة` before Driver enters Interpreter/backend work.
        دع مرجع_الأنواع: &سجل_أنواع = &الواجهة.الأنواع;
        دع الأنواع: سجل_أنواع = *مرجع_الأنواع;
        أرجع نتيجة_خفض_واجهة_مسير.ناجح(البرنامج, الأنواع);
    }
    | نتيجة_خفض_مسير.مرفوض(الخطأ) -> {
        أرجع نتيجة_خفض_واجهة_مسير.مرفوض(حول_فشل_خفض_إلى_تشخيصات(الخطأ));
    };
}

// Driver-facing observation of the lowering result. Driver owns sequencing and
// presentation only; it does not import or inspect the MSIR program payload.
عام عرف خفض_واجهة_مسير_ناجح(النتيجة: &نتيجة_خفض_واجهة_مسير): منطقي {
    طابق *النتيجة مع
    | نتيجة_خفض_واجهة_مسير.ناجح(_, _) -> {
        أرجع صواب;
    }
    | نتيجة_خفض_واجهة_مسير.مرفوض(_) -> {
        أرجع خطأ;
    };
}

عام عرف برنامج_خفض_واجهة_مسير(
    النتيجة: &نتيجة_خفض_واجهة_مسير
): اختيار برنامج_مسير {
    طابق *النتيجة مع
    | نتيجة_خفض_واجهة_مسير.ناجح(البرنامج, _) -> {
        أرجع اختيار.بعض(البرنامج);
    }
    | نتيجة_خفض_واجهة_مسير.مرفوض(_) -> {
        أرجع اختيار.لا_شيء();
    };
}

// Driver-facing entry identity query. The private Symbol→FunctionID lowering map never escapes MSIR.
عام عرف معرف_دالة_بدء_خفض_واجهة_مسير(
    النتيجة: &نتيجة_خفض_واجهة_مسير
): اختيار معرف_دالة_مسير {
    طابق *النتيجة مع
    | نتيجة_خفض_واجهة_مسير.ناجح(البرنامج, _) -> {
        أرجع معرف_دالة_بدء_برنامج_مسير(&البرنامج);
    }
    | نتيجة_خفض_واجهة_مسير.مرفوض(_) -> {
        أرجع اختيار.لا_شيء();
    };
}

عام عرف تشخيصات_خفض_واجهة_مسير(
    النتيجة: &نتيجة_خفض_واجهة_مسير
): اختيار مجموعة_تشخيصات {
    طابق *النتيجة مع
    | نتيجة_خفض_واجهة_مسير.ناجح(_, _) -> {
        أرجع اختيار.لا_شيء();
    }
    | نتيجة_خفض_واجهة_مسير.مرفوض(التشخيصات) -> {
        أرجع اختيار.بعض(التشخيصات);
    };
}
