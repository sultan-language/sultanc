/* Declares storage alignment helpers. */

#ifndef SULTANC_PRIVATE_SUPPORT_MEMORY_ALIGNMENT_H
/* Defines the sultanc private support memory alignment h macro. */
#define SULTANC_PRIVATE_SUPPORT_MEMORY_ALIGNMENT_H

#include <stddef.h>

/* Returns the align up. */
size_t __Align_Up__(size_t __Value__, size_t __Alignment__);

#endif
