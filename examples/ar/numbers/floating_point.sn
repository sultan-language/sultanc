نوع زوج = { اليسار: ع64, اليمين: ع64 };

عرف ادمج(القيمة: زوج): ع64 {
    أرجع (القيمة.اليسار * 2.0) + القيمة.اليمين;
}

عرف القلعة(): ص32 {
    دع الزوج: زوج = { اليسار: 20.0, اليمين: 2.0 };
    دع الناتج: ع64 = ادمج(الزوج);
    لو (الناتج == 42.0) { أرجع 42; };
    أرجع 1;
}
